name: CI

on:
  push:
    branches:
      - point-lio-with-grid-map

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - name: Run a script
        run: echo "Hello, GitHub Actions!"

